let Koa,Router,api,bodyParser;_076‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_076‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_076‍.w("./api",[["default",["api"],function(v){api=v}]]);_076‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);require('dotenv').config();




const mongoose = require('mongoose')

const app = new Koa();
const router = new Router();

const { PORT, MONGO_URI } = process.env;

mongoose.connect(MONGO_URI);
// mongoose.connect(MONGO_URI).then(() => {
// 	console.log("Connected to MongoDB");
// }).catch(e => {
// 	console.error(e);
// })

router.use('/api', api.routes());
router.get('/', ctx => {
	ctx.body = "home";
});

app.use(bodyParser());

app.use(router.routes()).use(router.allowedMethods());
app.listen(PORT, () => {
	console.log("Listening to port 80")
})